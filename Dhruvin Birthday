<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>

<title>Happy Birthday Dhruvin</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">

<style>
:root{
  --bg:#0f1115;
  --card:#161922;
  --text:#eaeaea;
  --muted:#a1a1aa;
  --gold:#c9a24d;
}

*{box-sizing:border-box;margin:0;padding:0}

body{
  min-height:100vh;
  background:radial-gradient(circle at top, #1b1f2a, var(--bg));
  font-family:'Inter',sans-serif;
  color:var(--text);
  display:flex;
  align-items:center;
  justify-content:center;
}

.wrapper{
  max-width:900px;
  padding:40px;
  text-align:center;
  animation:fadeUp 1.6s ease forwards;
}

h1{
  font-family:'Playfair Display',serif;
  font-size:3.2rem;
  letter-spacing:1px;
}

h1 span{
  color:var(--gold);
}

.sub{
  margin-top:12px;
  font-size:1.1rem;
  color:var(--muted);
}

.date{
  margin:35px auto;
  font-size:1.4rem;
  letter-spacing:4px;
  border:1px solid rgba(255,255,255,.15);
  display:inline-block;
  padding:12px 26px;
}

.card{
  margin-top:50px;
  background:linear-gradient(180deg, rgba(255,255,255,.03), rgba(255,255,255,.01));
  border:1px solid rgba(255,255,255,.08);
  border-radius:20px;
  padding:40px;
  backdrop-filter:blur(10px);
}

.card p{
  line-height:1.8;
  font-size:1.05rem;
  color:#d6d6d6;
}

.btn{
  margin-top:35px;
}

button{
  background:transparent;
  color:var(--gold);
  border:1px solid var(--gold);
  padding:14px 34px;
  border-radius:30px;
  font-size:0.95rem;
  letter-spacing:1px;
  cursor:pointer;
  transition:.3s;
}

button:hover{
  background:var(--gold);
  color:#0f1115;
}

.modal{
  position:fixed;
  inset:0;
  background:rgba(0,0,0,.6);
  display:flex;
  align-items:center;
  justify-content:center;
  opacity:0;
  pointer-events:none;
  transition:.4s;
}

.modal.active{
  opacity:1;
  pointer-events:auto;
}

.modal-box{
  background:var(--card);
  border-radius:20px;
  max-width:520px;
  padding:40px;
  text-align:left;
  position:relative;
  animation:scaleIn .6s ease forwards;
}

.close{
  position:absolute;
  top:16px;
  right:20px;
  font-size:1.4rem;
  cursor:pointer;
  color:var(--muted);
}

.modal-box h2{
  font-family:'Playfair Display',serif;
  margin-bottom:20px;
}

.modal-box p{
  line-height:1.8;
  color:#e5e5e5;
}

.signature{
  margin-top:30px;
  color:var(--gold);
  font-family:'Playfair Display',serif;
  font-size:1.1rem;
}

@keyframes fadeUp{
  from{opacity:0; transform:translateY(30px)}
  to{opacity:1; transform:translateY(0)}
}

@keyframes scaleIn{
  from{transform:scale(.9); opacity:0}
  to{transform:scale(1); opacity:1}
}
</style>
</head>

<body>

<div class="wrapper">
  <h1>Happy Birthday, <span>Dhruvin</span></h1>
  <div class="sub">For the man who carries ambition with grace.</div>

  <div class="date">13 · January</div>

  <div class="card">
    <p>
      Some people lead rooms.  
      Some people lead companies.  
      And some do both — while still knowing how to be gentle.
    </p>
  </div>

  <div class="btn">
    <button id="openLetter">Read something meant only for you</button>
  </div>
</div>

<div class="modal" id="modal">
  <div class="modal-box">
    <div class="close" id="close">&times;</div>
    <h2>Dhruvin,</h2>
    <p>
      You are calm where others rush.  
      Certain where others hesitate.  
      And soft in a world that keeps demanding hardness.
      <br><br>
      Today isn’t about achievements or titles.  
      It’s about the man behind them — the one who listens, protects, and still dreams.
      <br><br>
      May this year meet you with the same clarity and quiet strength you bring into every room.
    </p>
    <div class="signature">— Always rooting for you</div>
  </div>
</div>

<script>
const openBtn = document.getElementById("openLetter");
const modal = document.getElementById("modal");
const closeBtn = document.getElementById("close");

openBtn.onclick = ()=> modal.classList.add("active");
closeBtn.onclick = ()=> modal.classList.remove("active");
modal.onclick = e => e.target === modal && modal.classList.remove("active");
</script>

</body>
</html>
